#!/bin/bash

# Y-Blind 로그 확인 스크립트

echo "📋 Y-Blind 서비스 로그"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "💡 종료하려면 Ctrl+C를 누르세요"
echo ""

# 인자가 있으면 특정 서비스 로그만 표시
if [ -n "$1" ]; then
    echo "🔍 $1 서비스 로그:"
    docker-compose logs -f "$1"
else
    echo "🔍 모든 서비스 로그:"
    echo ""
    docker-compose logs -f
fi

